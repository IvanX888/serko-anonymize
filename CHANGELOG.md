# Журнал изменений

## v9.3 — жена ≠ муж: инициалы, девичьи фамилии, ВУ с «№» (2026-09-17)

### Исправлено
- **«И.И. Иванова» (жена) сливалась с мужем «Иванов И.И.»**: «Иванова» — омоним (жен. именит. = муж. род.п.), морфология всегда даёт lemma=иванов/gender=m, поэтому жена по инициалам получала плейсхолдер мужа. Теперь при порядке «инициалы + фамилия» женская поверхностная форма (-ова/-ева/-ина/-ская…) не склеивается с мужчиной (fio-engine, страж в bySurname-merge). Порядок «фамилия + инициалы» («Иванова П.И.») — без изменений: это обычно род.падеж мужчины/ребёнка.
- **Коллизия ключей персон**: формы «Иванов И.И.» и «И.И. Иванов» хранились под ключом «иванов||», поэтому любая фамилия-одиночка с тем же леммой наследовала чужую персону (подпись «/ И.И. Иванова /» улетала к мужу). Ключ форм с инициалами теперь составной: фамилия + поверхностная форма + инициалы + порядок (pre/post).
- **Голая фамилия из подписи прилипала к полному однофамильцу**: «(Петрова)» в перечне долей получала плейсхолдер представителя «Петрова Анна В.» (personByKey[«петрова||»] прописывался при инициал-склейке «А.В. Петрова»). Фамилия-одиночка без инициалов больше не наследует персону с именем/отчеством/инициалами; голые фамилии склеиваются только между собой.
- **«(в девичестве — Петрова)» прилипала к представителю**: новый пост-шаг anonymizer §2d — фамилия сразу после контекста «в/до девичества — » наследует персону предшествующего ФИО (жена), а не первого встречного носителя фамилии. Markdown-разметка (**…**) между тире и фамилией допускается.
- **Висячие инициалы у скобки**: «- Иванова (Петрова) И.И. — 1/4 доли» — хвостовые «И.И.» оставались незамаскированными. Пост-шаг §2e: непокрытые двойные инициалы в пределах 3 символов после ФИО-совпадения получают personId этой персоны.
- **ВУ/СТС с «№» между серией и номером**: «водительское удостоверение: 77 АА № 123456» не маскировалось («№» разрывал шаблон). re3 детектора doc допускает необязательный «№» внутри группы.

### Тесты
- Кейс 29 «Купля-продажа: жена/муж по инициалам, девичья фамилия, ВУ «77 АА № 123456»». Итог: 29 кейсов, P=R=F1=100%, TP=178.

## v9.2 — адреса через перенос строки, нулевые суммы (2026-09-16)

### Исправлено
- **Адрес с разорванным переносом в названии города**: «г. Санкт-\r\nПетербург, Невский пр-т, д. 88, кв. 21» маскировался куском — оставался «г. Санкт-». Продолжение города в cityBlock/re2 теперь допускает CRLF после дефиса и требует заглавной буквы у слова продолжения (чтобы «проспект» в «г. Москва\nпроспект Мира» не улетал в состав города).
- **«00 копеек» как [СУММА]**: при включённом типе money хвост «рублей 00 копеек» после прописной суммы маскировался, а сама сумма — нет. Нулевые значения пропускаются.

### Тесты
- Кейс 28 «Решение: «г. Санкт-\nПетербург» целиком, «00 копеек» не сумма». Итог: 28 кейсов, P=R=F1=100%, TP=162.

## v9.1 — исправление утечек (2026-09-16)

### Исправлено
- **Латинская транслитерация ФИО с инициалами** «(Ivanov I.I.)», «(Petrov P.P.)» не маскировалась: штраф «цифры/латиница» (−50) в scoreCandidate убивал короткие латинские формы (85−50=35 < порога). Теперь штрафуются только цифры и смесь кириллицы с латиницей в одном кандидате; чисто латинская форма — без штрафа.
- **Инициалы в подписях** «/ О.П. Смирнова /» маскировались частично (оставались «О.П.»): cleanWord(«О.П.») = «оп» коллидирует с оргформой «ОП» и давал штраф −100. Двойные инициалы «X.X.» теперь пропускаются в scoreCandidate, как одиночные.
- **Консистентность fio_lat**: латинская форма, слитая с кириллической персоной (anonymizer §2c), теперь получает тот же плейсхолдер ([ФИО-1] ([ФИО-1])), а в режиме псевдонимов — транслитерацию псевдонима («(Sokolov O.S.)»).

### Тесты
- Кейс 27 «Договор поручения»: (Ivanov I.I.), (Petrov P.P.), «/ О.П. Смирнова /». Итог: 27 кейсов, P=R=F1=100%, TP=161.

## v9.0 — полная переработка (2026)

### Движок
- **Морфологический движок v2**: полные парадигмы склонения фамилий, имён и отчеств (6 падежей). Обратный индекс словоформ для мгновенного анализа.
- **Белорусские фамилии**: классы -ич (Шушкевич), -ук/-юк (Шевчук), -ец (Кузнец, беглая гласная), -ко/-енко (несклоняемые). Мужские склоняются, женские — нет.
- **Нерегулярные имена**: Павел→Павла, Лев→Льва, Пётр→Петра и др.

### Валидация (новое)
- Контрольные суммы: **ИНН-10/12, СНИЛС, ОГРН/ОГРНИП, УНП РБ, р/с + БИК (ключевание), банковские карты (Лун), IBAN (mod-97)**.
- Личный (идентификационный) номер РБ: формат DDMMYYX + буква + 3 цифры + PB + цифра, проверка даты.
- Совпадения с подтверждённой контрольной суммой помечаются значком ✓.

### Юридический whitelist (новое)
- Номера судебных дел (`2-1234/2025`, `А56-12345/2024`), статьи кодексов (`ст. 393 ГК РБ`, `ст. 131-132 ГПК РФ`), номера договоров/доверенностей/счетов, пункты договора (`п. 1.2`) — **никогда не маскируются**.

### Режимы
- **Псевдонимы**: реалистичные фиктивные ФИО, консистентные для каждой персоны, склоняемые в падеж оригинала (включая подсказку падежа по предлогу для несклоняемых фамилий: «с Тарасовым Р.Д.»).
- **С восстановлением**: JSON-карта + обратная замена одной функцией.

### Качество
- Тестовый корпус из 6 юридических документов РФ/РБ с метриками P/R/F1.
- **P=100%, R=100%, F1=100%** (было: ~90% в v7).
- Контекстные слова («паспорт серия», «ИНН», «р/с», «зарегистрирован по адресу») больше не попадают в маску — скрываются только сами данные.

### Интерфейс
- Модульная архитектура (7 JS-модулей вместо одного монолита на 3800 строк).
- Группировка типов данных, пресеты «всё/ничего/по умолчанию».
- «Юр. лица» и «Суммы» по умолчанию выключены (для исков и договоров).
- Счётчик символов, тосты, улучшенный интерактивный редактор.

## v7.3 — предыдущая версия
- Монолитный HTML, MorphEngine + GrammarAnalyzer, базовые словари.
